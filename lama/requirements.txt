pyyaml
tqdm
numpy
easydict==1.9.0
scikit-image
scikit-learn
opencv-python
# NOTE: torchvision ops like NMS cause import errors on CPU-only builds.
# We do not pin torchvision here; it is installed in the top-level Dockerfile
# with a CUDA-enabled wheel matching the runtime PyTorch to avoid missing ops.
tensorflow
joblib
matplotlib
pandas
albumentations==0.5.2
hydra-core==1.1.0
pytorch-lightning==1.2.9
tabulate
kornia==0.5.0
webdataset
packaging
wldhx.yadisk-direct
timm
